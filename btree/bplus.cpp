#include "bplus.h"
#include <stdio.h>
#include <stdlib.h>

void
BPLUSTree::insert(int ent) {
    if (cur_root == NULL) {
        // initialize root with this element
        //cur_root = malloc
    }
}

int
BPLUSTree::find(int key) {
    return key;
}
